name: Main Build Workflow
on:
  push:
    branches:
      - develop
jobs:
  echo-job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Echo Hello
        run: echo "Hello"
        id: hello
  call-workflow-A:
    uses: .github/workflows/build.yml@develop
    uses: shahXrul/lantern/.github/workflows/build.yml@develop
    with:
      username: Houssem